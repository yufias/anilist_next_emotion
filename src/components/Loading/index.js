/** @jsxImportSource @emotion/react */
import { LoadingStyle } from "./LoadingStyle"

const Loading = () => {
    return(
        <div css={LoadingStyle.loading}>
        </div>
    )
}

export default Loading